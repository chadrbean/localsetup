# Secrets — never commit
.env
*.env
.env.*
!.env.example
!*.env.example

# Runtime artifacts
*.db
*.sqlite
*.log
*.lock

# Monitoring stack — local runtime state, never commit
monitoring/data/
monitoring/prometheus/bearer_token

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/

# OS
.DS_Store
google-sa-key.json
.omo/
gsc-mcp/client_secrets.json
gsc-mcp/service_account.json

# Claude Code: worktrees are real git checkouts and the lock is per-session state.
# .claude/skills/speckit-* IS tracked (agent pipeline onboarding contract).
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/settings.local.json
